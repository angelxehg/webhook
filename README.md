# simple-webhook

Simple Docker image bundling [webhook](https://github.com/adnanh/webhook), `curl`, `jq` and optional AWS CLI.

## Local development

Start docker-compose:

```shell
docker compose up -d --build
```
