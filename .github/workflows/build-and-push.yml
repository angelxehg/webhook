name: Build and deploy

on:
  push:
    branches:
      - main
    tags:
      - "v*"

jobs:
  build:
    name: Build
    runs-on: ubuntu-24.04

    env:
      REGISTRY: ghcr.io
      REPO: ${{ github.repository }}
      VERSION: ${{ github.ref_name }} # "main" or "v0.1.0"

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build webhook
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: webhook
          build-args: |
            AWS_ENABLED=false
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPO }}:${{ env.VERSION }}
          platforms: |
            linux/amd64
            linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build webhook (with AWS CLI)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: webhook
          build-args: |
            AWS_ENABLED=true
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPO }}:${{ env.VERSION }}-aws-cli
          platforms: |
            linux/amd64
            linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
